export function TopNav() {
  return <header className="border-b border-[var(--border)] bg-white px-6 py-4">
    <p className="text-sm font-semibold">CreditLens Research Console</p>
    <p className="text-xs text-slate-500">Probability is an estimate. Pricing depends on the loss and cost assumptions behind it.</p>
  </header>;
}
